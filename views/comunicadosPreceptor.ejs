<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/style.css">
    <title>Comunicados Enviados</title>
    <style>
        ul {
            list-style-type: none;
            padding: 0;
        }
        
        li {
            background-color: white;
            margin: 10px 0;
            padding: 20px;
            border-radius: 8px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            transition: transform 0.2s;
        }
        
        li:hover {
            transform: translateY(-5px);
        }
        
        li h3 {
            margin-bottom: 10px;
            font-size: 20px;
            color: #333;
        }
        
        li p {
            margin: 5px 0;
            color: #666;
        }
        
        li small {
            display: block;
            margin-top: 10px;
            font-size: 14px;
            color: #999;
        }
        
        a {
            display: inline-block;
            margin-top: 20px;
            padding: 10px 20px;
            background-color: #007BFF;
            color: white;
            border-radius: 5px;
            text-decoration: none;
            text-align: center;
        }
        
        a:hover {
            background-color: #0056b3;
        }
        

    </style>
</head>
<body>
    <h1>Comunicados Enviados por el Preceptor</h1>

    <h2>Filtrar por curso:</h2>
    <form action="/comunicados-preceptor" method="GET">
        <select name="cursoId">
            <% cursos.forEach(function(curso) { %>
                <option value="<%= curso._id %>">Año: <%= curso.anio %>, División: <%= curso.division %></option>
            <% }) %>
        </select>
        <button type="submit">Filtrar</button>
    </form>

    <h2>Comunicados:</h2>
    <% if (comunicados.length > 0) { %>
        <ul>
            <% comunicados.forEach(function(comunicado) { %>
                <li>
                    <strong>Título:</strong> <%= comunicado.titulo %><br>
                    <strong>Información:</strong> <%= comunicado.info %><br>
                    <strong>Curso:</strong> Año: <%= comunicado.curso.anio %>, División: <%= comunicado.curso.division %><br>
                    <strong>Fecha de Envío:</strong>
                    <% if (comunicado.createdAt) { %>
                        <%= comunicado.createdAt.toDateString() %>
                    <% } else { %>
                        Fecha no disponible
                    <% } %>
                </li>
            <% }) %>
        </ul>
    <% } else { %>
        <p>No se encontraron comunicados.</p>
    <% } %>

    <a href="/preceptor-dashboard">Volver al Dashboard</a>
</body>
</html>
